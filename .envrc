# Arquivo .envrc para auto-ativação do ambiente
# Requer direnv instalado: sudo apt install direnv

# Ativar ambiente conda quando entrar no diretório
use conda ./.conda

# Ou se criar ambiente nomeado:
# use conda trading-bot